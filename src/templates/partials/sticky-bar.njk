  <!-- Sticky Booking Bar -->
  <div class="sticky-bar" role="complementary" aria-label="{{ t.stickyBar.ariaLabel }}">
    <div class="container sticky-bar-inner">
      <div class="sticky-bar-info">
        <div>
          <div class="sticky-bar-price" data-base-price="{{ stickyPrice }}" data-base-price-original="{{ stickyOriginalPrice }}">
            {{ site.baseCurrency }} {{ stickyPriceFormatted }} <small>{{ site.baseCurrency }} {{ stickyOriginalPriceFormatted }}</small>
          </div>
          <div class="text-sm text-muted">{{ t.stickyBar.subText }}</div>
        </div>
      </div>
      <a href="#" class="btn btn-primary" data-booking>{{ t.stickyBar.bookNow }}</a>
    </div>
  </div>
